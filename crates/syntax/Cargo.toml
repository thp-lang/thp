[package]
name = "thp-syntax"
version.workspace = true
publish = false
edition.workspace = true
license-file.workspace = true
rust-version.workspace = true
description = "THP lexer, spanned AST, and recovering parser"

[dependencies]
thp-diagnostics = { path = "../diagnostics" }

[lints]
workspace = true

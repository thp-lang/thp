# Core language fixtures

These PHPT fixtures exercise THP language behavior end to end through the
compiler and reference VM.

Run them from the repository root:

```sh
cargo run -p thp-test -- tests/phpt/language
```

